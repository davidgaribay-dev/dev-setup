# Claude Code Harness Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Then source it before running harness commands:
#   source .env
#   cd orchestration && uv run harness all -c 2

# =============================================================================
# REQUIRED - Proxmox Authentication
# =============================================================================
# Generate at: Proxmox > Datacenter > Permissions > API Tokens
# Format: user@realm!token-name=token-secret
export TF_VAR_proxmox_api_token=""

# =============================================================================
# REQUIRED - Neo4j Deployment
# =============================================================================
# Initial admin password (set during first deployment)
export NEO4J_ADMIN_PASSWORD=""

# Connection password for MCP servers on Claude VMs
export NEO4J_PASSWORD=""

# =============================================================================
# REQUIRED - Core Services Deployment (Plane + Rewind)
# =============================================================================
# Generate these with openssl:
#   openssl rand -hex 32  (for SECRET_KEY)
#   openssl rand -hex 16  (for LIVE_SECRET_KEY)
#   openssl rand -base64 24  (for passwords)

export PLANE_SECRET_KEY=""
export PLANE_LIVE_SECRET_KEY=""
export PLANE_POSTGRES_PASSWORD=""
export PLANE_RABBITMQ_PASSWORD=""
export PLANE_MINIO_ACCESS_KEY=""
export PLANE_MINIO_SECRET_KEY=""

# Email for Caddy SSL certificate registration
export CADDY_EMAIL=""

# Core Services IP (default: 10.0.70.70)
# export CORE_SERVICES_IP="10.0.70.70"

# =============================================================================
# OPTIONAL - Claude VMs
# =============================================================================
# GitHub Personal Access Token for gh CLI pre-authentication
# Scopes needed: repo, read:org
# export ANSIBLE_GH_TOKEN=""

# Override default Neo4j IP (usually not needed)
# export NEO4J_IP="10.0.70.60"

# =============================================================================
# OPTIONAL - Plane MCP Server (configure after Plane is running)
# =============================================================================
# After deploying Plane and creating a workspace:
# 1. Login to https://10.0.70.70
# 2. Go to Settings > API Tokens
# 3. Generate an API key
# 4. Set these variables and redeploy Claude VMs with --skip-provision

# export PLANE_API_KEY=""
# export PLANE_WORKSPACE_SLUG=""
